[
  {
    "slug": "htmltopdfconverter",
    "comingSoon": false,
    "title": "HTML to PDF Converter",
    "company": "",
    "description": "Built a Next.js web application to allow users to convert HTML to PDF(s) both via the UI and programatically with Vercel infrastructure.",
    "tech": ["Next.js", "React", "Puppeteer ", "Stripe", "Resend", "MongoDB", "JWT", "Vercel", "Redis", "Material UI"],
    "period": "Feb 2025 — Sep 2025",
    "content": "<h2>Overview</h2>\n<p><a href=\"https://www.htmltopdfconverter.com.au\" target=\"_blank\" rel=\"noopener noreferrer\">HTML to PDF Converter</a> is a full-stack SaaS application for converting HTML files into print-ready PDFs. The project supports both a free web interface for one-off conversions and a paid developer API for programmatic PDF generation.</p>\n<img src=\"/projects/htmltopdfconverter/upload-tool.png\" alt=\"Upload interface\" />\n\n<h2>What I Built</h2>\n<p>The web tool lets users drag and drop up to 10 HTML files, customise page format, orientation, margins, scale, and output mode, then download either a single PDF, a merged PDF, or a ZIP of separate PDFs.</p>\n<p>The developer API adds account-based access, JWT API keys, usage tracking, Stripe subscriptions, and rate limiting. Developers can generate an API key from the dashboard and call a REST endpoint to convert HTML files from their own applications.</p>\n<img src=\"/projects/htmltopdfconverter/dashboard-api-keys.png\" alt=\"Developer dashboard\" />\n\n<h2>Technical Architecture</h2>\n<p>The app is built with Next.js App Router, React, TypeScript, MongoDB, NextAuth, Stripe, Upstash Redis, Vercel Blob, and Puppeteer. In development it uses full Puppeteer; in production it uses <code>puppeteer-core</code> with <code>@sparticuz/chromium</code> so Chromium can run inside Vercel's serverless environment.</p>\n<p>The conversion flow is:</p>\n<ol>\n  <li>User uploads one or more HTML files.</li>\n  <li>The API validates file type, file count, and file size.</li>\n  <li>Puppeteer renders each HTML document to PDF using the selected options.</li>\n  <li>Multiple PDFs are either merged with <code>pdf-lib</code> or archived with <code>archiver</code>.</li>\n  <li>The finished artifact is stored behind a session-based download route.</li>\n  <li>The UI streams progress updates back to the user using server-sent events.</li>\n</ol>\n\n<h2>Sticking Points</h2>\n<h3>Running Chromium on serverless infrastructure</h3>\n<p>The hardest part was making PDF rendering reliable in production. Local Puppeteer is straightforward, but deploying Chromium in a serverless environment requires a smaller runtime binary and launch flags that work within Vercel's constraints. I split the renderer so local development uses Puppeteer directly while production uses <code>puppeteer-core</code> and <code>@sparticuz/chromium</code>.</p>\n\n<h3>Giving users feedback during long conversions</h3>\n<p>Batch conversion can take several seconds, especially when users upload multiple complex HTML files. Instead of leaving the UI idle, the web route returns a streaming response and sends progress messages such as rendering, merging, archiving, and download preparation.</p>\n\n<h3>Supporting batch output cleanly</h3>\n<p>The app needed to handle three output cases: a normal single PDF, one merged PDF for multiple inputs, or a ZIP containing separate PDFs. I separated the PDF artifact creation logic from the route handler so the rendering, merging, zipping, and download metadata could be tested independently.</p>\n\n<h3>Protecting infrastructure costs</h3>\n<p>Because browser rendering is expensive, the app uses Upstash Redis rate limits for both the free web tool and the paid API. The public web interface is IP-limited, while the API is user-limited and tied to subscription state.</p>\n\n<h2>Result</h2>\n<p>The final product is a working HTML-to-PDF service with a free public converter, a paid API, account management, API key lifecycle controls, Stripe billing, and production-ready serverless rendering. It turns a deceptively simple feature into a complete SaaS workflow: upload, render, download, authenticate, bill, and protect the system from abuse.</p>\n<p>The developer documentation is available at <a href=\"https://www.htmltopdfconverter.com.au/docs\" target=\"_blank\" rel=\"noopener noreferrer\">htmltopdfconverter.com.au/docs</a>.</p>\n<img src=\"/projects/htmltopdfconverter/api-docs.png\" alt=\"API documentation\" />"
  },
  {
    "slug": "gymlogapp",
    "comingSoon": false,
    "title": "Gym Log - Strength Journal",
    "company": "",
    "description": "Built a mobile application using modern AI engineering techinques to complete, test and deploy a side project I've had in mind for 10+ years.",
    "tech": ["Expo", "React Native", "TypeScript", "Supabase", "NativeWind", "Tailwind", "Reanimated", "React Navigation", "Biome", "Jest"],
    "period": "April 2026 — June 2026",
    "content": "<h2>Gym Log</h2><p>A React Native mobile app for logging strength workouts — programs, sessions, personal bests, rest timers, and progress charts — built on Expo and Supabase.</p><img src=\"/projects/gym-log/login.png\" alt=\"Gym Log login screen\" /><h2>Overview</h2><p>Gym Log started as a personal itch: every gym tracking app I tried either buried the act of logging a set under three taps, or charged a subscription for features I wanted to own. So I built my own — a fast, opinionated logger that treats a workout like what it actually is: a list of exercises, each with a list of sets, each with reps and weight. Nothing more, nothing less, until the basics felt right.</p><p>Gym Log is now live on the <a href=\"https://apps.apple.com/au/app/gym-log-strength-journal/id6766238867\" target=\"_blank\" rel=\"noopener noreferrer\">App Store</a>, with more at <a href=\"https://gym-log-app.com/\" target=\"_blank\" rel=\"noopener noreferrer\">gym-log-app.com</a>.</p><h2>Background — Claude Code &amp; the Ralph Technique</h2><p>The first eighteen commits of this project — everything from the Expo scaffold through to a working auth flow, CRUD services, navigation, and all five core screens — weren't written by me at the keyboard. They were generated by running <a href=\"https://claude.com/claude-code\" target=\"_blank\" rel=\"noopener noreferrer\">Claude Code</a> inside an autonomous loop, using a workflow Geoffrey Huntley calls the <strong><a href=\"https://ghuntley.com/ralph-wiggum-as-a-software-engineer/\" target=\"_blank\" rel=\"noopener noreferrer\">Ralph technique</a></strong>.</p><p>Ralph, in its purest form, is a Bash loop: <code>while :; do cat PROMPT.md | claude-code; done</code>. You write a single prompt that points the model at a backlog (in my case a numbered list of user stories — US-001, US-002, …), give it the rules of engagement (one story per iteration, run the tests, commit on green, mark the story done), and then you let it run. The model picks the next unchecked story, implements it, verifies its own work, commits, and the loop fires again. You sleep, it ships.</p><p>What I found is that the technique works astonishingly well for the <em>boring foundational scaffold</em> of a project — the part where decisions are mostly mechanical and the cost of a wrong call is low. It generated a clean, typed, tested foundation in a single overnight run that would have taken me a long weekend to write by hand, and the disciplined one-commit-per-story log it left behind made the codebase easy to navigate afterwards. Where Ralph was less useful was the second half of the project: the redesign, the placeholder UX, the dark mode retrofit. Those required taste calls, lived experience using the app, and the kind of \"no, that feels wrong, undo it\" judgement that doesn't fit inside a deterministic loop. So the project is genuinely a hybrid — autonomous agent for the bones, hands-on for the soul — and I think that split is the honest answer to \"how much of this did the AI write.\"</p><h2>What I Built</h2><p>The app is structured around four screens that mirror how I actually use it at the gym:</p><img src=\"/projects/gym-log/programs.png\" alt=\"Programs list\" /><ul><li><strong>Workouts</strong> — reusable program templates (e.g. \"Push Day A\") that you instantiate into a session each time you train. Instantiated sessions pre-fill set fields with placeholder values from your last session of that template, so you can see what you lifted last time and either match or beat it with a single tap.</li><li><strong>Session detail</strong> — inline editing for everything: exercise name, set reps, set weight. No modals, no save buttons. Auto-save on blur. Exercises can be grouped into supersets or dragged to reorder mid-session, and a back-navigation guard catches you before you walk away from unsaved work. A configurable rest timer slides up between sets with haptics + a notification when it fires.</li><li><strong>Progress</strong> — weight progression and total-reps-per-session charts per exercise, with a date-range selector, tap-to-view tooltips, and a full-screen landscape mode for reading dense data.</li><li><strong>History &amp; Favourites</strong> — read-only past sessions with a \"Repeat workout\" action, plus a starred view that pins your favourite programs to the top.</li></ul><img src=\"/projects/gym-log/session.png\" alt=\"Session detail with rest timer\" /><img src=\"/projects/gym-log/history.png\" alt=\"Workout history\" /><img src=\"/projects/gym-log/progress.png\" alt=\"Progress charts\" /><p>Beyond the core loop, the app auto-detects personal bests (renamed to \"Personal Best\" mid-development after user-testing it on myself — \"PR\" reads as ambiguous), exports a session as a shareable image card, and can fire a daily streak reminder at a time you choose to nudge you back to the gym. Auth and storage are Supabase, with row-level security so each user only ever sees their own data.</p><img src=\"/projects/gym-log/account.png\" alt=\"Account screen\" /><img src=\"/projects/gym-log/how-to-use.png\" alt=\"How to use GymLog\" /><h2>Technical Architecture</h2><p><strong>Stack:</strong> Expo SDK 55, React Native 0.83, React 19, TypeScript, Supabase (Postgres + Auth + RLS), NativeWind v4 / Tailwind v3, React Navigation v7, Reanimated v4, Gifted Charts, Biome for lint/format, Jest + React Native Testing Library.</p><p>The data model is four tables: <code>profiles</code> (extending Supabase <code>auth.users</code>), <code>workouts</code>, <code>workout_exercises</code>, and <code>workout_sets</code>, each protected by row-level security so a user can only ever read or write their own rows. The first cut of the schema was actually only two tables — <code>workouts</code> carried its exercises and sets inline as a <code>jsonb</code> blob, on the assumption that every read was \"fetch the whole workout\" and a single round trip would be fastest. That held up until the Progress screen landed: charting weight progression for a single exercise across months of sessions meant pulling every workout, hydrating the JSON in JS, then filtering — which got noticeably laggy as my own history grew. Migrating to normalised <code>workout_exercises</code> and <code>workout_sets</code> tables (with indexes on <code>workout_id</code>, <code>exercise_id</code>, and exercise <code>name</code>) let those queries push filtering and joining down into Postgres where it belongs, and the per-exercise history sheet went from a visible pause to instant. Worth the migration; would have been worth doing up front in hindsight.</p><p>State is layered: a <code>SupabaseClient</code> singleton at the bottom, thin service modules (<code>authService</code>, <code>workoutService</code>) that own all I/O, then React Context providers (<code>AuthContext</code>, <code>WorkoutContext</code>, <code>ThemeContext</code>) that expose typed hooks to screens. No Redux, no Zustand — Context was sufficient because the data fan-out is shallow and writes are user-initiated.</p><p>Auth tokens persist via <code>expo-secure-store</code> (originally AsyncStorage; swapped early when I realised JWTs were sitting in plaintext). Theme preference also persists locally, with the write deferred off the interaction frame after profiling showed the toggle felt sticky on older devices.</p><h2>Sticking Points</h2><p><strong>The \"Programs vs. Workouts\" rename.</strong> The first version of the app had a single concept — a Workout — that you'd either create blank or duplicate. After living with it for a few weeks I realised what I actually wanted was a clean separation between the <em>template</em> (the program I follow week to week) and the <em>instance</em> (today's session). The redesign touched almost every screen: a two-zone Workouts list, a new \"Start\" action that instantiates a template, a <code>source_template_id</code> foreign key on instantiated workouts so I could pull last-session placeholders forward, and a soft-delete on empty sessions so abandoned starts didn't pollute history. Worth it — the app now matches the mental model.</p><p><strong>Placeholder UX.</strong> Pre-filling set inputs with last session's values sounds simple until you ask: are those values <em>the user's data</em>, or <em>a suggestion</em>? If they're data, saving an unedited session double-counts last week's lift. If they're a suggestion, the user needs a clear visual signal. I landed on rendering them as muted placeholder text (not real input values), tracking edited-vs-untouched state per set, and showing a banner on instantiated sessions until the first edit lands. Empty sessions on save get soft-deleted so the history stays honest.</p><p><strong>Dark mode without a rewrite.</strong> The app was built dark-first with hardcoded Tailwind colour classes everywhere. Retrofitting a theme system meant either a search-and-replace bloodbath or something smarter. I went with CSS custom properties: defined the colour palette as <code>--color-*</code> variables in <code>global.css</code>, redirected every Tailwind token through <code>var(--color-*)</code> in the config, and let NativeWind's CSS interop carry the values into native views. A single <code>ThemeProvider</code> swaps the variable values at the root and every styled component re-renders with the new palette — no per-component changes needed beyond a handful of imperative colour usages (charts, the rest timer bar) that had to be migrated by hand.</p><p><strong>Email confirmation and the App Store round-trip.</strong> Signup runs through Supabase email confirmation, and the confirmation link deep-links straight back into the app (<code>gymlog://auth/callback</code>) rather than dead-ending in a browser. Profiles are created by a Postgres trigger on <code>auth.users</code> instead of a client-side write, so a freshly confirmed account always lands with its profile row already in place — no race between \"account exists\" and \"profile exists.\" Getting that flow watertight took a couple of App Store resubmissions; the deep-link handling and the confirm-then-return path are exactly the kind of edge cases that only surface once a real reviewer, and real users, are in the loop.</p><p><strong>Shipping discipline.</strong> The commit log reads like two projects glued together: a methodical first pass (US-001 through US-018, then US-101 through US-108 — every commit a single user story) and a much messier \"make it real\" phase (redesign, lint cleanup, DB latency migration, polish). The structured phase got the bones right; the messy phase is where the app actually became something I'd recommend to a friend. I'd run it the same way again.</p><h2>Result</h2><p>What started as \"I'll build a tracker in a weekend\" turned into a fully-featured fitness app I use four times a week and that's now <a href=\"https://apps.apple.com/au/app/gym-log-strength-journal/id6766238867\" target=\"_blank\" rel=\"noopener noreferrer\">live on the App Store</a>. The discipline of building it user-story by user-story up front made the later, scrappier work — the redesign, the dark mode retrofit, the placeholder UX — far cheaper than it had any right to be, because the foundations didn't need to move.</p>"
  },
  {
    "slug": "soundcloud-mcp",
    "comingSoon": false,
    "title": "SoundCloud MCP — Festival Mix Finder",
    "company": "",
    "description": "A Model Context Protocol server on Cloudflare Workers that turns a 160-artist festival lineup into a SoundCloud playlist — one best DJ mix per artist — and lets an AI agent drive discovery, ranking, and playlist creation end to end.",
    "tech": [
      "TypeScript",
      "Cloudflare Workers",
      "Durable Objects",
      "Workers KV",
      "Model Context Protocol",
      "MCP SDK",
      "OAuth 2.0 / PKCE",
      "Hono",
      "Zod",
      "Vitest",
      "Wrangler",
      "Claude Code"
    ],
    "period": "June 2026",
    "content": "<h2>Overview</h2>\n<p>I went to <a href=\"https://www.dekmantelfestival.com\" target=\"_blank\" rel=\"noopener noreferrer\">Dekmantel</a> in Amsterdam back in 2015 and never quite got over it. I've been on their mailing list ever since, so when the 2026 time schedule landed in my inbox — four days, seven stages, somewhere north of 150 acts across the Amsterdamse Bos and the city — my first instinct was the same as every year: who are all these names I don't recognise, and what do they actually sound like?</p>\n<p>The honest answer is that I'm never going to hand-search a hundred-and-fifty artists on SoundCloud one by one. But I <em>would</em> happily listen to a playlist that did it for me — one great DJ mix from every artist on the bill, especially the ones I've never heard of. So I built the thing that could make that playlist programmatically, and then let an AI agent actually run it.</p>\n<p>The result is a <a href=\"https://soundcloud.com/dwbra/sets/dekmantel-2026\" target=\"_blank\" rel=\"noopener noreferrer\">public 105-track playlist</a> — one best mix per artist across the entire Dekmantel 2026 lineup — and, underneath it, a <strong>Model Context Protocol (MCP) server</strong> running on Cloudflare's edge that an assistant like Claude can drive to search SoundCloud, disambiguate artists, rank mixes, and build playlists on my behalf.</p>\n\n<h2>What is MCP, and why build one?</h2>\n<p><a href=\"https://modelcontextprotocol.io\" target=\"_blank\" rel=\"noopener noreferrer\">Model Context Protocol</a> is an open standard for handing an AI assistant a set of tools it can call — think of it as a typed API contract the model reads and invokes directly. Instead of writing a one-off script, I wrote a server that exposes SoundCloud as a toolbox: <code>search_tracks</code>, <code>resolve_artist</code>, <code>find_mixes</code>, <code>best_mix_per_artist</code>, <code>create_playlist</code>, and so on. Once it's connected, the model can plan and execute the whole curation task itself — and the same toolbox is reusable for anything else I want to do with my account.</p>\n<p>It came to 27 tools across three areas: <strong>discovery</strong> (search and resolve tracks, users, and playlists), <strong>DJ-mix intelligence</strong> (the artist-resolution and mix-ranking logic that makes the festival use case work), and <strong>engagement writes</strong> (like, repost, follow, comment, and full playlist CRUD).</p>\n\n<h2>How it works</h2>\n<p>The server is a Cloudflare Worker, and structurally it's three nested wrappers around that list of tools:</p>\n<ul>\n<li><strong>An OAuth provider at the edge</strong> handles the connection from the AI client and routes traffic: requests to <code>/mcp</code> go to the agent, everything else to the auth routes.</li>\n<li><strong>A per-user MCP agent</strong>, implemented as a Cloudflare <a href=\"https://developers.cloudflare.com/durable-objects/\" target=\"_blank\" rel=\"noopener noreferrer\">Durable Object</a>, holds the actual MCP server instance and is bound to one authenticated SoundCloud user. It speaks the Streamable HTTP transport that remote AI connectors expect.</li>\n<li><strong>A tool registry</strong>: each tool is a plain object — a name, a description, a <a href=\"https://zod.dev\" target=\"_blank\" rel=\"noopener noreferrer\">Zod</a> schema (which the SDK turns into the JSON Schema the model sees), and a handler. One loop registers all 27. Every handler is thin glue over a typed <code>SoundCloudClient</code>; none of the business logic lives in the tools themselves.</li>\n</ul>\n<p>The part that catches people out is that there are <em>two</em> OAuth flows stitched together. The AI client authorises against my server (my server is the authorization server, minting the token the assistant carries). My server, in turn, authorises against SoundCloud (now my server is the OAuth <em>client</em>). Connecting the tool runs both flows back to back, and the user's SoundCloud tokens are stored encrypted in Workers KV, refreshed on demand.</p>\n\n<h2>The actual hard part: turning a flyer into the right artists</h2>\n<p>A festival lineup is gloriously hostile to a computer. The bill doesn't say \"Shed\"; it says <em>\"Shed presents Rave Echoes\"</em>. It says <em>\"Ben UFO &amp; Call Super &amp; Objekt &amp; Pariah\"</em>, and <em>\"DJ Sprinkles' Deeperama\"</em>, and <em>\"Sass (Moxie &amp; Peach &amp; Saoirse &amp; Shanti Celeste)\"</em>. Before you can find anyone's best mix, you have to decide what each billing even <em>means</em>.</p>\n<p>So the first pass is a dedup with explicit rules: split back-to-back and collective billings into individual artists, treat \"X presents Y\" as the headliner X, and drop the things that aren't DJs at all (live bands, panel talks, sound systems). That collapsed ~150 raw billings down to <strong>164 unique artists</strong>.</p>\n<p>Then each name has to become a SoundCloud profile. The resolver scores candidates on a handful of signals — a permalink guess, exact-name match, and follower dominance — with genre as a bonus <em>only when it's present</em>, because real DJ mixes almost never carry genre or tag metadata. The rule I cared most about: <strong>never silently guess wrong</strong>. The resolver only auto-picks at high confidence; otherwise it returns ranked candidates and a reason — <code>unresolved</code>, <code>low_confidence</code>, or <code>no_mix</code> — and defers to a human. Of the 164 artists, 105 produced a confident best mix; the other 59 came back with honest reasons (and a few big names, like Jeff Mills and Ricardo Villalobos, genuinely just don't post long mixes on SoundCloud).</p>\n\n<h2>Sticking Points</h2>\n<h3>A successful write that reported itself as a failure</h3>\n<p>After deploying, I tested the <code>repost</code> tool against my real account and it returned <code>Unexpected end of JSON input</code> — an error. Except the repost had actually <em>worked</em>; it was sitting on my profile. SoundCloud answers some write requests with an empty <code>204 No Content</code> body, and my HTTP layer was unconditionally calling <code>response.json()</code> on every 2xx response — which throws on an empty body. So every successful \"unlike\", \"unfollow\", or \"unrepost\" was being surfaced to the user as an error. The fix is small — guard the empty/204 case before parsing — but I only caught it because I probed the live endpoint rather than trusting a green test suite, and the bug was invisible to tests that all mocked non-empty JSON bodies.</p>\n\n<h3>The rate-limit cascade that lied about who exists</h3>\n<p>Resolving all 150-odd artists in big batches tripped SoundCloud's rate limiter, and the failure mode was sneaky. Each artist resolution makes several API calls; when a burst hit the limit, the failed lookups were reported as <code>unresolved</code> — i.e. \"this artist doesn't exist.\" Ben UFO, one of the most famous DJs on the bill, came back \"unresolved\" in a batch of 23, then resolved instantly in a batch of 3. The limiter was a <em>burst</em> problem wearing the costume of a <em>data</em> problem. The fix was three-fold: smaller batches, a cap on how deep the per-artist track pagination can run, and an abort flag so that once any worker hits a 429, the rest of the batch stops instead of stampeding the API. When I later ran the whole branch through an adversarial code review, the reviewer independently fingered the exact same unbounded-pagination root cause — a satisfying confirmation.</p>\n\n<h3>An OAuth race I designed away</h3>\n<p>The upstream SoundCloud login uses a PKCE flow, and the original implementation stashed the PKCE verifier in Cloudflare KV, keyed by the OAuth <code>state</code> parameter. KV is eventually consistent, so when SoundCloud redirected straight back — because my account was already authorised, with no consent screen to slow things down — the callback's <em>read</em> could outrun the <em>write</em>, producing an intermittent \"expired or unknown state\" failure that a retry would fix. Rather than paper over it, I made the flow stateless: encrypt the verifier and the original request into the <code>state</code> parameter itself with AES-GCM, then decrypt it on the way back. No KV round-trip, no race, and a 600-second freshness check baked into the encrypted payload to preserve the old expiry behaviour.</p>\n\n<h2>How it was built</h2>\n<p>The whole thing was built test-first — 124 tests covering the ranking logic, the resolver's confidence contract, the OAuth handlers, and the HTTP edge cases — and then put through a full adversarial code review before merge, which is where the rate-limit and empty-body issues were pinned down and fixed under TDD. It was also a genuinely <em>agentic</em> build: I worked through it with <a href=\"https://claude.com/claude-code\" target=\"_blank\" rel=\"noopener noreferrer\">Claude Code</a>, and once the server was live the agent didn't just write the code — it <em>drove the tools against live SoundCloud</em> to produce the playlist: extracting the lineup from the festival flyers, running the dedup, calling the resolver across all 164 names, hand-recovering the ambiguous headliners, and creating the final playlist. Building the system and operating it were the same loop.</p>\n\n<h2>Result</h2>\n<p>A live MCP server on Cloudflare's edge, and a real artifact to show for it: a <a href=\"https://soundcloud.com/dwbra/sets/dekmantel-2026\" target=\"_blank\" rel=\"noopener noreferrer\">105-track Dekmantel 2026 playlist</a>, one best mix per artist, sequenced in festival-day order — exactly the thing I wanted in my inbox when the timetable dropped. It's now how I'm getting to know this year's lineup.</p>\n<p>For the record: this is a personal, educational project built on SoundCloud's official API with OAuth, run at low volume for my own listening — it backs off on rate limits, isn't operated as a public service, and the source stays private. The transferable parts are the engineering — the MCP server design, the dual-OAuth, the resolver, and the agentic build-and-run loop — and that's what this writeup is really about.</p>"
  }
]
