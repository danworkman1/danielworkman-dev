{
  "name": "tested-trappist",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "cf-typegen": "wrangler types",
    "predev": "pnpm cf-typegen",
    "dev": "astro dev",
    "prebuild": "pnpm cf-typegen",
    "build": "astro build",
    "postbuild": "cp .assetsignore dist/.assetsignore",
    "deploy": "pnpm build && pnpm exec wrangler deploy",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "node scripts/verify-edge.mjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.2",
    "@sentry/browser": "^10.69.0",
    "@tailwindcss/vite": "4",
    "astro": "^7.0.7",
    "tailwindcss": "4"
  },
  "devDependencies": {
    "@sentry/vite-plugin": "^5.4.0",
    "vitest": "^4.1.10",
    "wrangler": "4.107.0"
  }
}
