# Keep the root project explicit for pnpm 10, which is used by Cloudflare Builds.
packages:
  - "."

# pnpm 10+ reads the build-script allowlist from the workspace config.
onlyBuiltDependencies:
  - esbuild
  - sharp
  - workerd
